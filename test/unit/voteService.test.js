/* eslint-disable no-undef */
import '../../setup.js';
// import faker from 'faker';
// import voteService from '../../src/services/voteService.js';
// import voteRepository from '../../src/repositories/voteRepository.js';

describe('test votes', () => {
  // jest.spyOn(voteRepository, 'updateScoreById');
  it('test working!', () => {
    const a = 2;
    expect(a).toBe(2);
  });
});
